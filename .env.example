# Скопируйте в .env.local и подставьте значения.
# Локально: значения печатает `npm run db:start` (или `supabase status`).
# Продакшен: Project Settings → API в панели Supabase.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_KEY=<anon key>
